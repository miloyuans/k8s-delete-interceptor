diff --git a/delete_confirmation.go b/delete_confirmation.go
index 0000000..0000000 100644
--- a/delete_confirmation.go
+++ b/delete_confirmation.go
@@
-		keyboard = append(keyboard, []map[string]string{
-			{"text": "Rollback " + limitButtonText(entry.Name), "callback_data": rollbackCallbackData(entry.RollbackID)},
-		})
+		keyboard = append(keyboard, []map[string]string{
+			{
+				"text":          "回滚 " + limitButtonText(entry.Name),
+				"callback_data": rollbackCallbackData(rollbackActionApply, entry.RollbackID),
+			},
+			{
+				"text":          "下载 YAML",
+				"callback_data": rollbackCallbackData(rollbackActionDownload, entry.RollbackID),
+			},
+		})
 	}
